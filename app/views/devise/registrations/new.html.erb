<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h2><%= t("sin_up") %></h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="field">
        <%= f.label :name %><br />
        <%= f.text_field :name, autofocus: true, placeholder: true, required: true %>
      </div>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, placeholder: true, required: true %>
      </div>

      <div class="field">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password", placeholder: true, required: true %>
      </div>

      <div class="field">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: true, required: true %>
      </div>

      <div class="field">
        <%= f.label :date_of_birth %><br />
        <%= f.date_field :date_of_birth, autofocus: true, placeholder: true, required: true %>
      </div>

      <div class="field">
        <%= f.label :gender %><br />
        <%= f.radio_button :gender, "male" %> <%= f.label :gender, t("user.male") %>
        <%= f.radio_button :gender, "female" %> <%= f.label :gender, t("user.female") %>
      </div>

      <div class="field">
        <%= f.label :address %><br />
        <%= f.text_field :address, autofocus: true, placeholder: true, required: true %>
      </div>

      <div class="field">
        <%= f.label :phone %><br />
        <%= f.text_field :phone, autofocus: true, placeholder: true, required: true %>
      </div>

      <div class="actions">
        <%= f.submit t("user.sign_up"), class: "btn btn-primary" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
