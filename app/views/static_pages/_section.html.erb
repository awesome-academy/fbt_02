<section class="awe-section pd-0" id="box-search">
    <div class="box-search-wrapper">
      <div class="container">
        <!-- box-search -->
        <div class="box-search">
          <div class="form-item">
            <%= search_form_for @search do |f| %>
            <%= f.search_field :name_cont, class: "form_label" %>
          </div>
          <%= f.submit t("content.search"), name: nil, class: "md-btn md-btn--primary md-btn--pill" %>
          <% end %>
        </div>
      </div><!-- End / box-search -->
    </div>
  </div>
</section>
